# wrongsecrets

향후 의도적으로 취약한 워크로드를 온보딩하기 위한 placeholder입니다.

실제 구현 시 workload-specific asset은 이 디렉터리에 두고, Jenkinsfile, scripts, Helm/GitOps 구조, security gate policy, evidence model은 공통 Golden Path를 재사용합니다.

